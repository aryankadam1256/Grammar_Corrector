# Minimal dependencies for API inference (used in Docker)
# Omits training tools (wandb, tensorboard, errant, pytest) to keep image smaller

# Core ML - inference only
torch>=2.4.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.21.0
tokenizers>=0.13.0
sentencepiece>=0.1.99
safetensors>=0.4.0

# API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
numpy>=1.24.0
